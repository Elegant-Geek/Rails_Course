<!DOCTYPE html>
<html>
  <head>
    <title>Movies</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
<%# for the header call the header partial %>
  <%= render "layouts/header" %>

<%# NOTE: anything in the content class gets corectly "padded" due to "@extend .container;" in the .content CSS class. %>
  <div class="content">
    <%= render "layouts/flash" %>
    <%= yield %>
  </div>

<%# for the footer call the footer partial %>
  <%= render "layouts/footer" %>
  </body>
</html>
